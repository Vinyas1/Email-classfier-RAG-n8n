# Knowledge base docs

Drop .txt or .md files here - SOPs, runbooks, FAQ entries, anything an
agent would normally check before replying to a ticket. Each file gets
chunked and embedded alongside the historical tickets.

After adding files, rebuild the index:

    python manage.py build_kb

Example file: vpn-reset-procedure.md
